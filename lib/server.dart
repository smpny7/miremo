class Server {
  String title;
  String address;
  int port;
  String documentID;
  String iconUrl;
  int onlineMembers;
  int capacityMembers;

  Server(this.title, this.address, this.port, this.documentID, this.iconUrl,
      this.onlineMembers, this.capacityMembers);
}
